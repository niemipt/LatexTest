before_install:
- sudo apt-get update -qq
- sudo apt-get install texlive-latex-base
script:
- mkdir build
- pdflatex -output-directory build source/document.tex
deploy:
  provider: releases
  api_key:
    secure:
  file:
    - output/root.pdf
  skip_cleanup: true
  on:
    tags: true
